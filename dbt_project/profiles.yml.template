trial_activation:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: "LLMQLKG-OS30228"           # ← already confirmed correct
      user: "bamzz"        # ← your Snowflake login email/username
      password: "Q9GZRz3QLVGn8wu"    # ← your Snowflake password
      role: "ACCOUNTADMIN"                      # ← or ACCOUNTADMIN — your personal role
      warehouse: "TRIAL_WH"
      database: "SPLENDOR_ANALYTICS"
      schema: dbt_dev
      threads: 4
      client_session_keep_alive: false

# ──────────────────────────────────────────────────────────────────────────────
# INSTRUCTIONS
# ──────────────────────────────────────────────────────────────────────────────
# 1. Copy this file to: C:\Users\Bamzz\.dbt\profiles.yml
#    (create the .dbt folder if it doesn't exist)
#
# 2. Fill in the placeholders (angle brackets) with your Snowflake credentials.
#
# 3. To find your account identifier, open Snowflake and check:
#    Admin → Accounts → your account → copy the "account identifier"
#    It looks like: xy12345.us-east-1
#
# 4. Recommended: use environment variables instead of plaintext credentials:
#    user:     "{{ env_var('SNOWFLAKE_USER') }}"
#    password: "{{ env_var('SNOWFLAKE_PASSWORD') }}"
#
# 5. NEVER commit this file to git — it contains credentials.
#    It is already listed in .gitignore by default when using dbt init.
# ──────────────────────────────────────────────────────────────────────────────
